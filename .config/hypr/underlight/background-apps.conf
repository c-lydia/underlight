# Programs that should start in the background once per Hyprland session.
# Add one line per application, then reload Hyprland or log in again.
#
# Examples:
# exec-once = ~/.local/bin/underlight-background syncthing serve --no-browser
# exec-once = ~/.local/bin/underlight-background spotify --start-in-tray

